PORT=5000
ADMIN_PASSWORD=admin_kku_oas
ADMIN_EMAIL=admin_oas@kku.ac.th

# Google Sheets Configuration (Optional - Fallback to Local JSON if empty or invalid)
SPREADSHEET_ID=
GOOGLE_APPLICATION_CREDENTIALS=backend/credentials.json

# SMTP Configuration (Optional - Fallback to Local Log if empty or invalid)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SENDER=
